
# install_eabi_arm_gcc

Role for installing the arm-none-eabi-gcc compiler on the target system.

## Usage
Add the role to your playbook:

```yaml
- hosts: all
  roles:
    - baremetal.install_eabi_arm_gcc
```
